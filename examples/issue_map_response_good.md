We thank the reviewers for the careful feedback. The strict zero-shot claim is now separated from calibrated diagnostics, and the Protocol Summary states each row's role, scope, threshold and label use, and evidence pointer.

For bCeM, we add a cost comparison covering proposal-only, model-only, verifier-only, and routed variants with memory class, latency relative to a named baseline, and route ratio. For FEoB, we diagnose negative cases and broader use by distinguishing accuracy, cost, routing, prompt, and prior-sensitivity failures.

For MekP, threshold and label use are reported as protocol attributes rather than hidden tuning. For y76H, the main result is label-free and the Revision map makes the claim easy to audit.
