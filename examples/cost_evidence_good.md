We added a compact cost row for the deployment concern. Proposal-only has light memory, fast latency, and route ratio N/A. VLM-only is the latency baseline with VLM memory and 100% VLM route. Verifier-only uses the same VLM memory with latency vs baseline 1.4x and 100% VLM route. Routed hybrid uses VLM plus proposal memory, latency relative to the VLM-only baseline, and a 35/65 proposal/VLM route ratio.
