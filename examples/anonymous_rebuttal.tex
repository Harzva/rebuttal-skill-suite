% Anonymous toy rebuttal used only to test suite validators.
We clarify that strict G2D$(1\theta)$=68.85 is the deployable result: it uses a fixed, label-free zero-shot rule with no validation labels.
Verifier=64.19 isolates the mechanism without changing the strict protocol.
Full-benchmark fixed-pair diagnostics use one shared pair with explicit label use = shared pair; they are diagnostic evidence, not individually tuned.
Calibrated G2D$(2\theta)$=67.53 is reported as sensitivity analysis outside strict deployment claims.
For cost, we report mem / latency vs baseline / route ratio for proposal-only, model-only, verifier-only, and routed variants.
For weak cases, we separate accuracy, cost, routing, prompt, and prior-reliability diagnoses.
