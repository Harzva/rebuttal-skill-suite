We thank FEoB, bCeM, y76H, and MekP for the careful feedback. The revised response separates the strict zero-shot claim from diagnostic analyses: the deployable result uses one fixed threshold without labels, while calibrated or fixed-pair analyses are reported only as sensitivity evidence.

For protocol clarity, the revision map will state each row's role, scope, label use, and evidence pointer. For cost clarity, we will include a compact comparison covering proposal-only, model-only, verifier-only, and routed variants with memory class, latency relative to the named baseline, and route ratio when applicable.

For negative cases and broader use, we will report when failures are due to accuracy, cost, routing sensitivity, prompt sensitivity, or proposal-prior misses. We will also bound the claim to closed-vocabulary recognition settings with a reliable proposal prior; open-label or shifted settings require additional validation.
