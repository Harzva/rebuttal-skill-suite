Strict G2D$(1\theta)$ is reported, but no fixed or label-free deployment protocol is stated.
The fixed-pair result uses per-dataset tuned thresholds and is presented as the main claim.
G2D$(2\theta)$ is the deployable main claim.
Cost is discussed without route ratio or latency baseline.
